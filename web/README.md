# Application workspace

This directory contains the deployable SvelteKit application for EmailSmith. See the [project README](../README.md) for features, privacy behavior, setup, and deployment guidance.

Run all Node and npm commands from this directory:

```sh
npm install
npm run dev
```

Useful validation commands are `npm test`, `npm run check`, `npm run lint`, and `npm run build`.
